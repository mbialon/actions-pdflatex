name: main
on:
  push:
    branches:
      - master
env:
  IMAGE: docker.pkg.github.com/mbialon/actions-pdflatex/texlive:latest
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: docker login docker.pkg.github.com --username mbialon
      - run: docker build -t $IMAGE .
      - run: docker push $IMAGE
